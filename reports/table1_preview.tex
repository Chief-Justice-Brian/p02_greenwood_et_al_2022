% Standalone preview for the Table 1 artifacts in _output/.
%
% The generated .tex files are bare tabular FRAGMENTS designed to be \input
% into the final report; they cannot compile on their own (no \begin{document},
% and they rely on the booktabs package). Open THIS file instead -- or run
% `doit compile_latex` -- to get a viewable PDF of both tables.
\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{caption}

\begin{document}

\begin{table}[htbp]
  \centering
  \small
  \caption{Replicated Table 1 of Greenwood, Hanson, Shleifer and S{\o}rensen
  (WP 20-130). The layout and reported moments follow the original table;
  every number shown here is recomputed from our public-data reconstruction.
  Debt and price rows use their corresponding complete sector-pair samples,
  and percentile thresholds use the pooled common forecasting sample.}
  \input{../_output/table1_summary_stats.tex}
\end{table}

\begin{table}[p]
  \centering
  \scriptsize
  \caption{Divergence report: our replicated values next to the paper's
  published values. Differences are expected because the public-data equity
  series substitute for the paper's paid GFD and Bloomberg inputs and because
  public sources have been revised since the paper's data vintage.}
  \input{../_output/table1_comparison.tex}
\end{table}

\end{document}
